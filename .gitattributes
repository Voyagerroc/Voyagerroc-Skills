# Keep line endings stable across OSes so generated catalog artifacts do not
# drift between Windows checkouts and Linux CI.
* text=auto eol=lf
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.pdf binary
